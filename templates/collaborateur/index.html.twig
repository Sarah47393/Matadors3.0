{% extends 'back.office.html.twig' %}

{% block title %}Collaborateur{% endblock %}

{% block body %}
    <h1>Collaborateur</h1>
    <div class="pcoded-search-box" style="position: relative; ">
  
        <input  type="text" id="searchEnt" class="form-control" placeholder="Search">
    


    </div>
         <button onclick="myFunction()">Click me</button>
    <p id="demo"></p>

<script>
function myFunction() {
    let searchString = document.getElementById('searchEnt').value; // recuperation de l'input de la recherche
        window.location = ('http://127.0.0.1:8000/evenement/order1/'+searchString

);
}
</script>

        
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCollaborateur</th>
                <th>PrenomCollaborateur</th>
                <th>Role</th>
                <th>NumeroTel</th>
                <th>Entreprise</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for collaborateur in collaborateurs %}
            <tr>
                <td>{{ collaborateur.id }}</td>
                <td>{{ collaborateur.NomCollaborateur }}</td>
                <td>{{ collaborateur.PrenomCollaborateur }}</td>
                <td>{{ collaborateur.Role }}</td>
                <td>{{ collaborateur.NumeroTel }}</td>
                <td>{{ collaborateur.Entreprise }}</td>
                <td>
                    <a href="{{ path('collaborateur_show', {'id': collaborateur.id}) }}">Afficher les détails</a>
                    <a href="{{ path('collaborateur_edit', {'id': collaborateur.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<a class=" btn btn-success " href="{{ path('collaborateur_new') }}">Ajouter un nouveau collaborateur</a>
<a class="btn btn-success  " href="{{ path('statistiques') }}">Les Staistiques</a>
{% endblock %}
